# QUALITY-005: Audit Remaining transition:all Usage

**Status**: Complete
**Created**: 2026-03-05
**Source**: BACKLOG.md (from POLISH-001)

---

## 1. Problem Statement

Audit CSS source files for remaining `transition: all` declarations that could cause unintended side effects. POLISH-001 fixed `.btn` and `.project-card__link` but spawned this task to verify no other instances remain.

## 2. Approach

Full codebase search for `transition: all` and `transition:.*\ball\b` patterns across all files.

## 3. Findings

**CSS source files (`css/*.css`)**: Zero occurrences of `transition: all`. All 15+ transition declarations already use explicit property lists.

**HTML files (`index.html`, `404.html`)**: Matches found in inlined critical CSS — these are build artifacts generated by Critters from the (already clean) source CSS. Regenerated on every `npm run build`.

**Documentation files**: References to `transition: all` are historical context in DONE.md, BACKLOG.md, and archived plan files.

## 4. Acceptance Criteria

- [x] No `transition: all` remains in CSS source files — **confirmed: zero occurrences**
- [x] Each transition explicitly lists animated properties — **confirmed: all 15+ declarations use explicit lists**
- [x] Existing behavior preserved — **confirmed: 198/198 Playwright tests passed (Chromium, Firefox, WebKit)**

## 5. Key Discoveries

- POLISH-001 was thorough — no remaining `transition: all` in any CSS source file
- No code changes required; this was a verification-only audit

## 6. Future Improvements

1. Add a Stylelint rule to prevent future `transition: all` declarations (e.g., `declaration-property-value-disallowed-list` for `transition: all`)
2. Consider documenting the "no transition: all" convention in CLAUDE.md conventions section

### Execution Log

#### 2026-03-05 — PHASE: Audit
- Searched all CSS source files for `transition: all` — zero matches
- Searched entire codebase for broader pattern — only docs/HTML build artifacts
- All 198 Playwright tests passed across 3 browsers

#### 2026-03-05 — PHASE: Complete
- No code changes needed
- Tests passing: yes (198/198)
- User approval: received

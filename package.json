{
  "name": "goodalex223-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio website for Alexey Minakov",
  "scripts": {
    "build": "postcss css/main.css --output dist/style.css",
    "watch": "postcss css/main.css --output dist/style.css --watch"
  },
  "devDependencies": {
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.1.0"
  }
}
